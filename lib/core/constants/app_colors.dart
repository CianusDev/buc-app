import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // empêche de créer une instance, c'est juste un conteneur
  static const Color primary = Color(0xFF212121);
  static const Color secondary = Color(0xFFF5F5F5);
  static const Color background = Color(0xFFFFFFFF);
  static const Color foreground = Color(0xFF212121);
  static const Color destructive = Color(0xFFE53935);
}
